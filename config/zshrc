# My own customization
[ -f ~/.my_zsh ] && \. ~/.my_zsh
command -v rbenv > /dev/null && eval "$(rbenv init - --no-rehash zsh)"

# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/hchen1/.lmstudio/bin"
